<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".modules.login.fragment.SetPasswordSucceedFragment">

    <TextView
        android:id="@+id/tv_succeed_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="25dp"
        android:layout_marginTop="25dp"
        android:drawableLeft="@drawable/ic_setting_password_succeed"
        android:drawablePadding="@dimen/small"
        android:gravity="center_vertical"
        android:text="设置成功，前往登录(5s)"
        android:textColor="@color/text_black"
        android:textSize="@dimen/text_medium" />

    <TextView
        android:id="@+id/tv_login"
        style="@style/MainButtonMedium.RedNormal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/small"
        android:layout_marginRight="@dimen/small"
        android:text="立即登录" />
</LinearLayout>